var server = require('./server');
var mocha = require('mocha');
var assert = require('assert');
var mongoose = require('mongoose');

// describe('connection with mongoDB', function() {
	
// 	it('should say connected', function(done) {
		
// 		// check if connected
// 		assert.equal(mongoose.connection.readyState,1);

// 		done();

// 	});

// });

describe('checking api', function() {
	
	it('should get the card data', function() {
		
	});

	it('should create card', function() {
		
	});

	it('should delete card with some name or id', function() {
		
	});

	it('should get particular card from id', function() {
		
	});

});